// Ambiente di produzione: cambiare questi due valori con l'indirizzo
// pubblico/reale del backend (es. https://api.miogiro.it/api).
export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000/api',
  socketUrl: 'http://localhost:3000',
};
